export {};

// lab-02
declare global {
    interface Window {
        __initialLoaderTimers?: number[];
    }
}

// rehearsal-01
declare global {
    interface Window {
        __rehearsal01InitialLoaderTimers?: number[];
    }
}

// rehearsal-02
declare global {
    interface Window {
        __rehearsal02InitialLoaderTimers?: number[];
    }
}
